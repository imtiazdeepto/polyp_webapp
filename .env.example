# Backend
CHECKPOINT_PATH=/app/best.pth
MAX_FILE_SIZE_MB=10
PORT=8000

# Render / cloud: direct download URL for best.pth (Hugging Face, S3, etc.)
# CHECKPOINT_URL=https://huggingface.co/your-user/your-repo/resolve/main/best.pth

# Frontend — Docker Compose (internal service name)
API_URL=http://backend:8000/predict

# Render — set automatically via render.yaml fromService
# BACKEND_HOST=polyp-backend.onrender.com

# Local Streamlit (no Docker)
# API_URL=http://localhost:8000/predict
